server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Aplicação de página única: qualquer rota desconhecida devolve o index e o roteador do
    # Angular resolve no navegador.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
